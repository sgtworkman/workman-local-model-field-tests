# Adversarial audit remediation — 2026-07-11

An independent Claude Fable 5 review returned `MAJOR REVISION` on v0.1.0. The project accepts that verdict.

## Corrected in the hardening branch

| Finding | Repair |
|---|---|
| Non-reproducible public leaderboard | Removed the leaderboard claim. Historical operator records are separated from runner-generated artifacts. |
| Porous sanitizer | Added embedded private-address detection, real credential prefixes, Linux home paths, tailnet hosts, and normalized camelCase/kebab-case sensitive keys. |
| Incomplete CI privacy boundary | Reused the same privacy scanner in CI and added RFC1918, CGNAT, link-local, reserved, and ULA address coverage. |
| Mislabeled speed | Renamed the non-streaming metric to request-level output throughput and aggregate it as total completion tokens divided by total request wall time. |
| Keyword-gaming scorer | Added word boundaries, structured start decisions, regex rules, and adversarial negative fixtures. |
| Missed reasoning field | Added `reasoning_content` detection. |
| Quality wipeout exited zero | Added a pass-rate threshold and distinct request-error/quality exit codes. |
| Native tool call marked empty | Normalize native `tool_calls` into the tool-shape evaluator. |
| Tail percentile overclaim | Use linear interpolation, report sample count, and suppress p95 below 20 samples. |
| Universal no-think controls | Made no-think controls explicit with `--no-think`. |
| Action supply-chain tags | Pinned GitHub Actions to full commit SHAs. |

## Still held

- Cross-model rankings until every row is generated by the same admitted public battery.
- Cross-hardware speed rankings.
- Decode-speed, TTFT, and inter-token claims until the streaming measurement path lands.

## Verification standard

Every repair requires a regression test. Live model success does not replace deterministic fixtures, and passing fixtures do not turn this small battery into a universal intelligence benchmark.
