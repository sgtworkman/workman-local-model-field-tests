# Security

Do not open a public issue for a credential leak or exploitable vulnerability.

Use GitHub's private vulnerability reporting for this repository. Revoke any exposed credential before reporting it.

Benchmark result files must not contain API keys, authorization headers, endpoint URLs, private addresses, internal hostnames, usernames, or personal filesystem paths. Run the sanitizer before publication.
